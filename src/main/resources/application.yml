# server:
#   port: 8888

# spring:
#   # profile settings
#   #   config:
#   #     activate:
#   #       on-profile: dev

#   # mariadb settings
#   datasource:
#     driver-class-name: org.mariadb.jdbc.Driver
#     url: jdbc:mariadb://localhost:3306/inspire
#     username: root
#     password: 5534

#   jpa:
#     hibernate:
#       # create, create-drop, update, none
#       ddl-auto: create
#       # 콘솔출력
#     show-sql: true
#     properties:
#       hibernate:
#         # h2
#         # dialect: org.hibernate.dialect.H2Dialect
#         # mariadb
#         dialect: org.hibernate.dialect.MariaDBDialect
#         format_sql: true

#   # redis
#   # docker run -d --name redis-container -p 6379:6379 redis:7.2
#   # docker exec -it redis-container redis-cli
#   # ping
#   # docker run -d --name redis-insight -p 8001:5540 redis/redisinsight:latest
#   # http://localhost:8001
#   # Host: redis 또는 host.docker.internal , port: 6379

#   data:
#     redis:
#       host: localhost
#       port: 6380
#       password:
#       timeout: 6000ms

# # mybatis settings
# # mybatis:
# #     mapper-locations: /mappers/**/*Mapper.xml
# #     config-location:  classpath:/mappers/config/mybatis-config.xml

# jwt:
#   secret: jslim-super-secret-key-for-jwt-12345678

server:
  port: 8888

spring:
  # profile settings
  #   config:
  #     activate:
  #       on-profile: dev

  # mariadb settings
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: ${SPRING_DATASOURCE_URL:jdbc:mariadb://localhost:3306/inspire}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:123456789}

  jpa:
    hibernate:
      # create, create-drop, update, none
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:create}
      # 콘솔출력
    show-sql: ${SPRING_JPA_SHOW_SQL:true}
    properties:
      hibernate:
        # h2
        # dialect: org.hibernate.dialect.H2Dialect
        # mariadb
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true

  # redis
  # docker run -d --name redis-container -p 6379:6379 redis:7.2
  # docker exec -it redis-container redis-cli
  # ping
  # docker run -d --name redis-insight -p 8001:5540 redis/redisinsight:latest
  # http://localhost:8001
  # Host: redis 또는 host.docker.internal , port: 6379

  data:
    redis:
      host: ${SPRING_REDIS_HOST:redis}
      port: ${SPRING_REDIS_PORT:6379}
      password:
      timeout: 6000ms

# mybatis settings
# mybatis:
#     mapper-locations: /mappers/**/*Mapper.xml
#     config-location:  classpath:/mappers/config/mybatis-config.xml

jwt:
  secret: jslim-super-secret-key-for-jwt-123456789
